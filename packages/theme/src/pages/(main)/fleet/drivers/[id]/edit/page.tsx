import React from 'react';
import DriverForm from '../../components/DriverForm';

export default function EditDriverPage() {
    // DriverForm will read the ":id" param from the route and switch to edit mode
    return <DriverForm />;
}
